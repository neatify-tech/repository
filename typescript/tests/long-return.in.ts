return ["default-src 'none'", `script-src ${join(baseSources, ["'unsafe-inline'"])}`, `style-src ${join(baseSources, ["'unsafe-inline'"])}`, `img-src ${join(baseSources, ['data:', 'blob:'])}`, `font-src ${join(baseSources, ['data:'])}`, `connect-src ${join(connectSources)}`, `media-src ${join(baseSources, ['data:', 'blob:'])}`, `frame-src ${join(baseSources)}`, "base-uri 'none'", "object-src 'none'"].join('; ')
