		tool.put(
			"description",
			"Run the Polymr-managed Groovy script resolved by path. Use the `" + TOOL_READ_SCRIPT_METADATA_FS + "` tool to get the schema for the expected input and output. These scripts are managed by Polymr and are not accessible through generic filesystem tools; use Polymr script tools only."
		);
