 String value = normalize(configuration.getRegion()) + "\n"
                       + normalize(configuration.getEndpoint()) + "\n"
                       + normalize(configuration.getAccessKeyId()) + "\n"
                       + normalize(configuration.getSecretAccessKey()) + "\n"
                       + normalize(configuration.getSessionToken()) + "\n"
                       + normalize(configuration.getPathStyleAccessEnabled()) + "\n";
