name = "markdown"
version = "0.1.0"
formatter = "core/markdown/main.rhai"

# Grammar source: https://github.com/MDeiml/tree-sitter-markdown
# Grammar tag: none
# Grammar commit: bd2d09e

extensions = [".md", ".markdown"]

[treesitter]
language = "markdown"
binary = "core/treesitter/{os}/{arch}/markdown.{ext}"
