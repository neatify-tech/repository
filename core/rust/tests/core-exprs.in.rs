async fn work(opt:Option<i32>,arr:[i32;3],mut v:Vec<i32>)->i64{
let mut sum=0;
loop{if sum>10 {break;} sum+=1;continue;}
while let Some(x)=opt {sum+=x;break;}
let a=arr[ 0 ];
let r=0..=10;
let t=(1 ,2,3 );
let x=1 as i64;
let y = async { x + 1 }.await;
unsafe{*(&mut v[0])=1;}
sum as i64
}
